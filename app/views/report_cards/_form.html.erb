<%= form_for(@report_card) do |f| %>
  <% if @report_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report_card.errors.count, "error") %> prohibited this report_card from being saved:</h2>

      <ul>
      <% @report_card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :UID %><br>
    <%= f.text_field :UID %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
